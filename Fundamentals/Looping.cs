class Loops
{
    public void LearnLoops()
    {
        // for (int i = 1; i < 11; i++)
        // {
        //     Console.WriteLine("Nepal");
        // }

        // for (byte i = 1; i < 101; i++)
        // {
        //     Console.Write(i + " ");
        // }

        // CW - Print even numbers less than 500
        //Console.WriteLine("Even Numbers less than 500 are as follows:");
        // for (int num = 0; num < 500; num++)
        // {
        //     if (num % 2 == 0)
        //     {
        //         Console.Write(num + " ");
        //     }
        // }


        // CW - Print all number less than 500 which are multiple of 3 and 5
        //Console.WriteLine("Numbers divisible by 3 and 5 less than 500 are as follows: ");
        // for (int i = 0; i < 500; i++)
        // {
        //     if (i % 3 == 0 && i % 5 == 0)
        //     {
        //         Console.Write(i + " ");
        //     }
        // }

        // byte n = 1;
        // while (n <= 100)
        // {
        //     Console.Write(n + " ");
        //     n++;
        // }

        // While - use case
        string key = "y";
        while (key == "y")
        {
            Console.WriteLine("Nepal");
            key = Console.ReadLine();
        }
    }
}